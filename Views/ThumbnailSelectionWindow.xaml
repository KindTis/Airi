<Window x:Class="Airi.Views.ThumbnailSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="썸네일 선택" Width="1120" Height="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#0F111C" Foreground="#E8ECF9"
        WindowStyle="None" ShowInTaskbar="False"
        PreviewKeyDown="OnWindowPreviewKeyDown">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="썸네일 후보 선택" FontSize="22" FontWeight="SemiBold" Foreground="White"/>
            <TextBlock Text="이미지 한 장을 선택하거나 후보 전체를 다시 생성하세요."
                       Margin="0,6,0,0" Foreground="#96A1C2"/>
        </StackPanel>

        <ListBox x:Name="CandidateList" Grid.Row="1"
                 Background="Transparent" BorderThickness="0"
                 SelectionMode="Single" IsTabStop="False"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 KeyboardNavigation.TabNavigation="Continue"
                 KeyboardNavigation.DirectionalNavigation="Contained"
                 SelectionChanged="OnCandidateSelectionChanged"
                 PreviewKeyDown="OnCandidatePreviewKeyDown">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Focusable" Value="True"/>
                    <Setter Property="IsTabStop" Value="True"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="FocusBorder" BorderBrush="Transparent"
                                        BorderThickness="2" CornerRadius="14" Padding="2">
                                    <Border x:Name="SelectionBorder" BorderBrush="Transparent"
                                            BorderThickness="3" CornerRadius="11"
                                            Background="#1A1E2F" Padding="5">
                                        <ContentPresenter/>
                                    </Border>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="SelectionBorder" Property="BorderBrush" Value="#3B82F6"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding Image}" Stretch="Uniform"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button x:Name="RegenerateButton" Content="다시 생성"
                    Width="110" Height="34"
                    Style="{StaticResource PrimaryActionButtonStyle}"
                    Click="OnRegenerateClick"/>
            <Button x:Name="CancelButton" Content="취소"
                    Width="110" Height="34" Margin="12,0,0,0"
                    Style="{StaticResource SecondaryActionButtonStyle}"
                    Click="OnCancelClick"/>
            <Button x:Name="ConfirmButton" Content="선택"
                    Width="110" Height="34" Margin="12,0,0,0"
                    Style="{StaticResource PrimaryActionButtonStyle}"
                    IsDefault="True" IsEnabled="False" Click="OnConfirmClick"/>
        </StackPanel>
    </Grid>
</Window>
